<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%-include("../partials/cabecalho.ejs")%>
    <div class="container">
    
        <h1><%= usuario._id ? "Atualizar" : "Cadastrar" %></h1>
    <form action="<%= usuario.id ? "/usuario/editar" : "/usuario" %>" method="post">
        <input type="hidden" name="_id" value="<%=usuario._id %>">
        <div>
            <label class="form-label">email:</label>
            <input value="<%=usuario.email %>"  class="form-control" type="text" name="email" required>
        </div>

        <div>
            <label class="form-label">nome:</label>
            <input value="<%=usuario.nome %>" class="form-control" type="text" name="nome" required>
        </div>

        <div>
            <label class="form-label">senha:</label>
            <input value="<%=usuario.senha %>" class="form-control" type="password" name="senha" required>
        </div><br>
        <div>
            <button type="submit" class="btn btn-success">Cadastrar</button>
            <a class="btn btn-primary" href="/usuario/login">ja possui conta?</a>

        </div>
        
    </form>
</div>
<%-include("../partials/rodape.ejs")%>
</body>
</html>